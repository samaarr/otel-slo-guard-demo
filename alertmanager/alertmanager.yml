global:
  resolve_timeout: 1m

route:
  receiver: webhook
  group_by: ["alertname", "service"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1m

receivers:
  - name: webhook
    webhook_configs:
      - url: http://alertmanager-webhook:8080/
        send_resolved: true
